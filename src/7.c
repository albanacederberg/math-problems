#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main() {
    int a = 5;
    int b = 8;
    double c = (a + b) / 2;
    printf("The average of %d and %d is %.1f\n", a, b, c);
    return 0;
}
